{% extends './templates/Step.jinja' %}
{% set name = 'Step' %}

{% block public %}
    /**
     * Step interface
     */
    virtual bool transform(float *source) = 0;

    /**
     * Reset step state
     */
    virtual void reset() {
        // step-specific code
    }

{% endblock %}