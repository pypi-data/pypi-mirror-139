from setuptools import setup, find_packages
from os.path import join, dirname

setup(
    name= 'VTG_con_ast_visualizer',
    version='1.4',
    packages=["my_lib"],
    long_description="Library to build ast for fibonacci function",
)
