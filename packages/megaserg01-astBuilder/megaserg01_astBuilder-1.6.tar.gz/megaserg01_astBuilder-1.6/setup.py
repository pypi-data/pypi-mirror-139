from setuptools import setup, find_packages
from os.path import join, dirname

setup(
    name='megaserg01_astBuilder',
    version='1.6',
    packages=["HW_1"],
    long_description="Library to build ast for fibonacci function",
)
