from setuptools import setup

setup(name='DirTreeGenerator',
      version='1.0',
      description='This Module can generate tree of files in directory.',
      packages=['treeGen'],
      author_email='max.taran.210@mail.ru',
      zip_safe=False)
